
var i;
var die;
var numSides = prompt("How many sides does the die have?");
var numDie = prompt("How many die are you utilizing at the moment?");

    for (i = 0; i < numDie, i++;) {
        die = Math.random(1, numSides) * 6;
        console.log(parseInt(die));
    }

console.log(die);